FROM python:3.11-slim

WORKDIR /app

COPY requirements-anchor.txt .
RUN pip install --no-cache-dir -r requirements-anchor.txt

COPY u_decision_api_v1.py .
COPY u_anchor_addition.py .

EXPOSE 8000

CMD ["uvicorn", "u_decision_api_v1:app", "--host", "0.0.0.0", "--port", "8000"]
